version: '3.8'

services:

  techmove-api:
    build:
      context: ./TechMovePOE.API
      dockerfile: Dockerfile
    ports:
      - "5001:8080"

  techmove-web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:8081"
    depends_on:
      - techmove-api